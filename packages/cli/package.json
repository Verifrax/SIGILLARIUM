{
  "name": "sigillarium",
  "version": "0.0.0",
  "description": "SIGILLARIUM CLI",
  "type": "module",
  "bin": {
    "sigillarium": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "scripts",
    "vendor/core",
    "dist/**",
    "scripts/**",
    "package.json"
  ],
  "license": "Apache-2.0",
  "scripts": {
    "build": "npx -y tsc -p tsconfig.json && node scripts/postbuild.mjs",
    "test": "node dist/cli.js --help >/dev/null 2>&1 || true",
    "prepack": "npm run build",
    "postpack": "node scripts/postpack.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
