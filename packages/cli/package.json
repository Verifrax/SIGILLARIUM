{
  "name": "sigillarium",
  "version": "0.0.0",
  "description": "SIGILLARIUM CLI",
  "type": "module",
  "bin": {
    "sigillarium": "dist/cli.js"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"const fs=require(\\\"fs\\\");const f=\\\"dist/cli.js\\\";const b=fs.readFileSync(f);const sh=\\\"#!/usr/bin/node\\\\n\\\"; if(!b.slice(0,sh.length).equals(Buffer.from(sh))) fs.writeFileSync(f, Buffer.concat([Buffer.from(sh), b])); fs.chmodSync(f, 0o755);\"",
    "test": "node dist/cli.js --help >/dev/null 2>&1 || true"
  },
  "dependencies": {
    "@sigillarium/core": "0.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
